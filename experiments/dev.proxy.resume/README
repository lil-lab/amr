Development proxy experiment. This experiment resumes learning from a saved snapshot. This includes not initializing the update procedure (e.g., AdaGrad), skipping initialization and starting from an existing model.

Stages:
	log model
	training (data set: amr-release-1.0-training-proxy.txt)
	log model
	testing (data set: amr-release-1.0-dev-proxy.txt)

Experiments:
	dev.proxy.resume.exp -- single machine experiment.
	dev.proxy.resume.dist.exp -- distributed experiment.
